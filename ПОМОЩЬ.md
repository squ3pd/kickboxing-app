# 🆘 Помощь по установке и запуску

## 📋 Содержание
1. [Установка Node.js](#установка-nodejs)
2. [Установка зависимостей](#установка-зависимостей)
3. [Запуск сервера](#запуск-сервера)
4. [Настройка API URL](#настройка-api-url)
5. [Деплой на хостинг](#деплой-на-хостинг)
6. [Решение проблем](#решение-проблем)

---

## 1. Установка Node.js

Если npm не установлен, нужно установить Node.js:

### Windows:
1. Скачайте Node.js с https://nodejs.org/
2. Установите LTS версию (рекомендуется)
3. Перезапустите терминал
4. Проверьте установку:
   ```bash
   node --version
   npm --version
   ```

### Mac:
```bash
brew install node
```

### Linux:
```bash
sudo apt-get update
sudo apt-get install nodejs npm
```

---

## 2. Установка зависимостей

После установки Node.js выполните:

```bash
npm install
```

Это установит все необходимые пакеты:
- express (веб-сервер)
- cors (для CORS)
- sqlite3 (база данных)

---

## 3. Запуск сервера

### Локально (для тестирования):

```bash
npm start
```

Сервер запустится на `http://localhost:3000`

### Проверка работы:

1. Откройте браузер: `http://localhost:3000`
2. Должна открыться ваша страница
3. Откройте консоль разработчика (F12)
4. Попробуйте добавить спортсмена
5. Проверьте, что в консоли нет ошибок

---

## 4. Настройка API URL

### Для локальной разработки:

В файле `app.js` найдите строку (около строки 55):

```javascript
const API_BASE_URL = window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1' 
    ? 'http://localhost:3000/api' 
    : 'https://your-server-url.com/api';
```

Для локальной работы ничего менять не нужно - уже настроено.

### Для продакшена (после деплоя):

Замените `https://your-server-url.com/api` на реальный URL вашего сервера.

Например:
```javascript
const API_BASE_URL = window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1' 
    ? 'http://localhost:3000/api' 
    : 'https://kickboxing-app.railway.app/api'; // Ваш URL
```

---

## 5. Деплой на хостинг

### Вариант 1: Railway (Самый простой) ⭐

1. Зарегистрируйтесь на https://railway.app (можно через GitHub)
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Railway автоматически определит Node.js проект
6. Дождитесь деплоя (1-2 минуты)
7. Нажмите на проект → Settings → Generate Domain
8. Скопируйте URL (например: `https://kickboxing-app.railway.app`)
9. Обновите `API_BASE_URL` в `app.js` на этот URL + `/api`

**Преимущества:**
- ✅ Бесплатный план (500 часов/месяц)
- ✅ Автоматический деплой из GitHub
- ✅ База данных SQLite работает
- ✅ HTTPS автоматически

### Вариант 2: Render

1. Зарегистрируйтесь на https://render.com
2. New → Web Service
3. Подключите GitHub репозиторий
4. Настройки:
   - Build Command: `npm install`
   - Start Command: `npm start`
5. Нажмите "Create Web Service"
6. Дождитесь деплоя
7. Скопируйте URL и обновите `API_BASE_URL`

### Вариант 3: Vercel (только фронтенд) + отдельный сервер для API

Vercel не поддерживает SQLite напрямую, поэтому нужен отдельный сервер для API.

---

## 6. Решение проблем

### Проблема: "npm не распознан"

**Решение:** Установите Node.js (см. раздел 1)

### Проблема: "Cannot find module 'sqlite3'"

**Решение:**
```bash
npm install sqlite3 --save
```

Или переустановите все зависимости:
```bash
rm -rf node_modules
npm install
```

### Проблема: Порт 3000 занят

**Решение:** Измените порт

В `server.js` найдите:
```javascript
const PORT = process.env.PORT || 3000;
```

Или установите переменную окружения:
```bash
# Windows PowerShell
$env:PORT=3001; npm start

# Windows CMD
set PORT=3001 && npm start

# Mac/Linux
PORT=3001 npm start
```

### Проблема: Данные не сохраняются

**Проверьте:**

1. Сервер запущен? (должна быть строка "🚀 Сервер запущен на порту 3000")
2. API URL правильный? (проверьте в консоли браузера)
3. Нет ошибок в консоли? (F12 → Console)
4. База данных создалась? (должен появиться файл `database.sqlite`)

### Проблема: "CORS error" в браузере

**Решение:** CORS уже настроен в `server.js`, но если проблема есть:

В `server.js` проверьте:
```javascript
app.use(cors());
```

Должно быть в начале файла.

### Проблема: Telegram User ID не определяется

**Решение:** 

В `app.js` проверьте получение User ID. Если не работает, можно использовать временный ID для тестирования.

Для тестирования можно временно использовать:
```javascript
telegramUserId = 'test_user_123';
```

---

## 📞 Дополнительная помощь

Если проблема не решена:

1. **Проверьте консоль браузера** (F12 → Console)
   - Там будут сообщения об ошибках
   - Скопируйте текст ошибки

2. **Проверьте консоль сервера**
   - Там тоже будут ошибки
   - Скопируйте текст ошибки

3. **Проверьте файлы:**
   - `server.js` - сервер запускается?
   - `app.js` - API URL правильный?
   - `database.sqlite` - создался файл?

4. **Пришлите:**
   - Текст ошибки из консоли
   - Скриншот проблемы (если возможно)
   - Что именно не работает

---

## ✅ Чеклист перед запуском

- [ ] Node.js установлен (`node --version` работает)
- [ ] npm установлен (`npm --version` работает)
- [ ] Зависимости установлены (`npm install` выполнен)
- [ ] Сервер запускается (`npm start` работает)
- [ ] Браузер открывает `http://localhost:3000`
- [ ] В консоли браузера нет ошибок
- [ ] Файл `database.sqlite` создался
- [ ] Можно добавить спортсмена
- [ ] Данные сохраняются

---

## 🚀 Быстрый старт (кратко)

```bash
# 1. Установите Node.js (если не установлен)
# https://nodejs.org/

# 2. Установите зависимости
npm install

# 3. Запустите сервер
npm start

# 4. Откройте браузер
# http://localhost:3000

# 5. Проверьте работу
# - Добавьте спортсмена
# - Проверьте консоль (F12)
```

---

Удачи! 🎉

